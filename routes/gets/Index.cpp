#include <fstream>
#include "Index.h"

void servePage() {
    std::ifstream indexHtml ("../../assets/index.html", std::ios::in | std::ios::binary | std::ios::out);

    // if (indexHtml.is_open()) {
    //     while (in)
    // }
}